import React, { Component } from 'react';
import './Loading.css';

class Loading extends Component {
  render() {
    return (
      <p className="Loading">
        Loading
      </p>
    );
  }
}

export default Loading;
